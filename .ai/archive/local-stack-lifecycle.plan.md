# Local Stack Lifecycle Cleanup

## Task Source

User request: ensure the local startup script stops every resource it launched and detects resources left behind by an earlier run.

## Implementation Task

- [x] Add managed process discovery and recursive shutdown.
  Files: `replicadb-server/frontend/scripts/local-api-processes.sh`, `replicadb-server/frontend/scripts/local-api-processes.test.sh`
  Changes: Detect ReplicaDB API and Maven launchers plus Vite and npm processes belonging to the checkout. Stop descendants before their roots, wait for termination, and escalate to SIGKILL when necessary.
  Tests: Bash syntax check and focused shell test covering discovery and shutdown order.

- [x] Integrate managed cleanup into local startup.
  Files: `replicadb-server/frontend/scripts/start-local.sh`
  Changes: Detect stale managed processes and the dedicated PostgreSQL container before port selection, request one interactive confirmation, label newly created containers, and recursively clean API, frontend, and PostgreSQL on normal exit, errors, or handled signals.
  Tests: Non-interactive stale-resource guard and local admin E2E lifecycle validation.

- [x] Document the resource ownership and cleanup behavior.
  Files: `replicadb-server/frontend/README.develop.md`
  Changes: Document recursive cleanup, stale-resource confirmation, unrelated-resource protection, and the SIGKILL limitation.
  Tests: `git diff --check` and documentation review.

## Execution Retrospective (auto-generated by /itx-code)

### Plan Accuracy

- Tasks completed as planned: 3/3 (100%).
- Tasks that required plan adjustment: 0/3 (0%).
- Test loop iterations: 4 total (first-pass, first-pass, first-pass, first-pass).

### Gaps Encountered

None - plan executed as designed.

### Patterns Discovered

- Managed local ownership: identify processes by application command, checkout working directory, and resource role; identify containers by a dedicated name and labels.
- Recursive local shutdown: terminate descendants before their launcher and verify disappearance before considering cleanup complete.
