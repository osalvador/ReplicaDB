# Implementation Plan: Local API Startup Guard

## Task Source

User request: detect existing local ReplicaDB API instances before local startup, ask before stopping them, and abort safely when confirmation is unavailable.

## Implementation Task

- [x] Add local API process detection and controlled shutdown.
  Files: `replicadb-server/frontend/scripts/local-api-processes.sh`, `replicadb-server/frontend/scripts/start-local.sh`
  Changes: Detect only `org.replicadb.server.ReplicaDbServerApplication` processes using the `api` profile and the current checkout directory. Display PID, port, and start time. Ask for confirmation in an interactive terminal; abort without killing processes when no terminal is available or confirmation is declined. Stop confirmed processes before selecting ports.
  Tests: Simulated helper test, Bash syntax check, and real non-interactive startup check with existing local APIs.

- [x] Document the local startup behavior.
  Files: `replicadb-server/frontend/README.develop.md`
  Changes: Explain duplicate-scheduler prevention, interactive confirmation, non-interactive abort behavior, and treatment of unrelated port users.
  Tests: `git diff --check` and documentation review.

## Execution Retrospective (auto-generated by /itx-code)

### Plan Accuracy

- Tasks completed as planned: 2/2 (100%).
- Tasks that required plan adjustment: 0/2 (0%).
- Test loop iterations: 2 total (first-pass, second-pass).

### Gaps Encountered

None — mini-plan executed as designed.

### Patterns Discovered

- Local process ownership: identify managed processes by application class, profile, and checkout working directory before offering termination.
